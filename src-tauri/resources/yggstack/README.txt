Place bundled sidecar files for release builds here.
Expected path after install:
resources\yggstack\yggstack.exe

The app prefers the embedded Yggstack bridge compiled into the main executable.
If the bridge is unavailable, it falls back to this bundled sidecar.
